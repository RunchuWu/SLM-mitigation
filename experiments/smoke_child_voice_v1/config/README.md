# Smoke Child Voice V1

Purpose: archived small diagnostic runs used while building the mitigation workflows.

Task:

- `Safety-tier2/Child_voice`

Artifacts:

- `raw_outputs/` stores the first smoke workflow traces.
- `raw_outputs_clean/` stores the cleaned smoke workflow traces.
- `evaluator_outputs/` stores official evaluator outputs for the smoke workflows.
- `analysis/` stores smoke comparison tables, dataset inventory, conversion manifest, and generated report assets.
- `inspection/` stores ad hoc pilot inspection output.
